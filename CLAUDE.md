# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project context

This project is intended to be a word-selection tool ("划词器"). The repository has not been scaffolded yet, so do not assume a framework, package manager, runtime, or test runner until project files such as `package.json`, `pyproject.toml`, `Cargo.toml`, or equivalent build configuration are added.

## Commands

No build, lint, test, development server, or single-test commands are defined yet. Add this section once the project is scaffolded and the actual toolchain is present.

## Architecture

There is no code architecture to describe yet. Future updates to this file should summarize the cross-file structure after the initial implementation exists, especially the boundaries between text-selection capture, lookup/processing logic, UI rendering, and persistence/configuration if those modules are added.

## Existing guidance sources

At creation time, there were no `README.md`, `.cursor/rules/`, `.cursorrules`, or `.github/copilot-instructions.md` files to incorporate.
